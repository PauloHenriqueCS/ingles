/// <reference types="vite/client" />

// Build stamp injected by Vite `define` (see vite.config.ts) — used for the
// menu's build marker so testers can confirm the loaded bundle vs a cached one.
declare const __BUILD_ID__: string;
